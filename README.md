# GitHub Action - Tag Poetry version bump

This returns `true` if either:

- The version of a poetry-managed project has changed on the default branch, or
- a tag matching the version was just pushed

